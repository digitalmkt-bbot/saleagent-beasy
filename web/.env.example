# ถ้า deploy frontend แยกจาก backend (เช่น Railway คนละ service)
# ตั้งค่านี้เป็น URL ของ backend + /api  เช่น https://your-api.up.railway.app/api
# ถ้าอยู่โดเมนเดียวกัน (Docker/nginx) ไม่ต้องตั้ง — จะใช้ /api อัตโนมัติ
VITE_API_URL=
